<%-include('partials/header.ejs'); %> 
<body>

    <%-include('partials/navbar.ejs'); %>

    <div class="container">
        <%Cadastro.forEach((item) => { %>
        <div class="card mb-4">
            <div class="card-header">
                <h3>ID da pergunta: <%=item.id%></h3>
               
            </div>
            <div class="card-body">

                <h5>Nome: <%=item.nome%></h5>
                <h5>Sobrenome: <%=item.sobrenome%></h5>
                <h5>Descricão: <%=item.descricao%></h5>
                <h5>Criado em: <%=item.createdAt%></h5>
                <h5>Atualizado em: <%=item.updatedAt%></h5>
    
            </div>

            <a class="btn btn-danger m-auto" href="/admin/list/<%=item.id%>">Detalhes</a>
            <a class="btn btn-danger m-auto mt-4" href="/admin/delete/<%=item.id%>">Deletar</a>
        </div>
        <%})%>
        
    </div>

    

  
       
  

   
</body>
<%-include('partials/footer.ejs'); %>
